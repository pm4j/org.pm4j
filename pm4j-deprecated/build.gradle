apply from: '../pm4j.gradle'
dependencies {
    compile project(':pm4j-core')
    // todo: Base classes for tests do not belong in src/test but in a separate project in src/main
    testCompile project(':pm4j-core').sourceSets.test.output
}
